spring:
  application:
    name: jpa-hibernate

  datasource:
    url: jdbc:mysql://localhost:3306/employees?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
    username: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

#logging:
#  level:
#    org:
#      hibernate:
#        SQL: DEBUG

